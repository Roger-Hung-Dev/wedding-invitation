# 專案級 MCP server 的密鑰「變數名」樣板（本檔進版控，只登記鍵名、不放真值）。
# .mcp.json 內以 ${VAR} 引用；切勿把密鑰寫死進 .mcp.json。
#
# ⚠️ 真值請填進 .claude/settings.local.json 的 "env" 區，「不是」 .env ——
#    Claude Code 不會自動載入 .env，.mcp.json 的 ${VAR} 只從 Claude Code 行程
#    自身的環境變數展開。真值放 .env 的話，server 會收到字面的字串 "${SOME_API_KEY}"，
#    錯誤訊息看起來像值填錯（辨識特徵：訊息裡出現 ${...}）。
#    填法：{"env": {"SOME_API_KEY": "真值"}, "enableAllProjectMcpServers": true}
#    改完必須重啟 Claude Code，server 才會以新環境重新啟動。
#
# 例：
# SOME_API_KEY=
